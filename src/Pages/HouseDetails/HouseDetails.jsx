

const HouseDetails = ({ details }) => {


  console.log('details here ', details.price_range).

    return(
      <div>
        <h1>  </h1>
        <h2> {details.price_range} </h2>
      </div>
    );
};

export default HouseDetails;